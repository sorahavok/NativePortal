<p id="notice"><%= notice %></p>

<% if(true) %>
  <%= render 'article', entry: @entry, abbreviate: false %>
<% else %>
  <p>
    <b>Section:</b>
    <%= @entry.section.title %>
  </p>

  <p>
    <b>Title:</b>
    <%= @entry.title %>
  </p>

  <p>
    <b>Url:</b>
    <%= @entry.url %>
  </p>

  <p>
    <b>Content:</b>
    <%= @entry.content %>
  </p>
  <%= link_to 'Edit', edit_entry_path(@entry) %> |
<% end %>

<%= link_to 'Back', @entry.section %>
