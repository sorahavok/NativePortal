<h1 class="text-center">All Entries</h1>

<% @entries.each do |entry| %>
  <%= render 'article', entry: entry, abbreviate: true %>
<% end %>

<br />
<% if user? %>
  <%= link_to 'New Entry', new_entry_path %>
<% end %>
